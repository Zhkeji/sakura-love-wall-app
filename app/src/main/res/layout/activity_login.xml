<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:background="@drawable/bg_gradient_auth">

    <!-- 浮动光点 -->
    <View android:layout_width="80dp" android:layout_height="80dp"
        android:background="@drawable/circle_floating"
        android:layout_gravity="top|start" android:layout_marginStart="30dp" android:layout_marginTop="100dp"
        android:alpha="0.15" />
    <View android:layout_width="50dp" android:layout_height="50dp"
        android:background="@drawable/circle_floating"
        android:layout_gravity="top|end" android:layout_marginEnd="50dp" android:layout_marginTop="200dp"
        android:alpha="0.1" />
    <View android:layout_width="120dp" android:layout_height="120dp"
        android:background="@drawable/circle_floating"
        android:layout_gravity="bottom|start" android:layout_marginStart="60dp" android:layout_marginBottom="150dp"
        android:alpha="0.08" />

    <!-- 登录表单 -->
    <LinearLayout android:id="@+id/loginForm"
        android:layout_width="match_parent" android:layout_height="wrap_content"
        android:layout_gravity="center" android:orientation="vertical"
        android:layout_marginHorizontal="32dp" android:padding="32dp"
        android:background="@drawable/bg_glass_card">

        <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="🌸" android:textSize="48sp" android:layout_gravity="center" />

        <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="欢迎回来" android:textSize="26sp" android:textStyle="bold"
            android:textColor="@color/white" android:layout_gravity="center"
            android:layout_marginTop="12dp" />

        <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="登录你的樱花表白墙账号" android:textSize="14sp"
            android:textColor="#B0FFFFFF" android:layout_gravity="center"
            android:layout_marginTop="4dp" android:layout_marginBottom="28dp" />

        <!-- 用户名 -->
        <EditText android:id="@+id/etUsername"
            android:layout_width="match_parent" android:layout_height="52dp"
            android:background="@drawable/bg_input_glass"
            android:hint="用户名" android:textColor="@color/white"
            android:textColorHint="#80FFFFFF"
            android:paddingHorizontal="20dp" android:textSize="15sp"
            android:inputType="text" android:maxLines="1"
            android:drawableStart="@drawable/ic_user" android:drawablePadding="12dp"
            android:drawableTint="#80FFFFFF" />

        <!-- 密码 -->
        <EditText android:id="@+id/etPassword"
            android:layout_width="match_parent" android:layout_height="52dp"
            android:background="@drawable/bg_input_glass"
            android:hint="密码" android:textColor="@color/white"
            android:textColorHint="#80FFFFFF"
            android:paddingHorizontal="20dp" android:textSize="15sp"
            android:inputType="textPassword" android:maxLines="1"
            android:layout_marginTop="16dp"
            android:drawableStart="@drawable/ic_lock" android:drawablePadding="12dp"
            android:drawableTint="#80FFFFFF" />

        <!-- 登录按钮 -->
        <Button android:id="@+id/btnLogin"
            android:layout_width="match_parent" android:layout_height="52dp"
            android:background="@drawable/btn_gradient_round"
            android:text="登 录" android:textColor="@color/white"
            android:textSize="16sp" android:textStyle="bold"
            android:layout_marginTop="24dp" />

        <!-- 切换注册 -->
        <TextView android:id="@+id/tvSwitchToRegister"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="注册账号" android:textSize="14sp"
            android:textColor="#B0FFFFFF" android:layout_gravity="center"
            android:layout_marginTop="20dp" android:padding="8dp" />
    </LinearLayout>

    <!-- 注册表单 -->
    <LinearLayout android:id="@+id/registerForm"
        android:layout_width="match_parent" android:layout_height="wrap_content"
        android:layout_gravity="center" android:orientation="vertical"
        android:layout_marginHorizontal="32dp" android:padding="32dp"
        android:background="@drawable/bg_glass_card"
        android:visibility="gone">

        <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="✨" android:textSize="48sp" android:layout_gravity="center" />

        <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="加入我们" android:textSize="26sp" android:textStyle="bold"
            android:textColor="@color/white" android:layout_gravity="center"
            android:layout_marginTop="12dp" />

        <TextView android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="创建你的樱花表白墙账号" android:textSize="14sp"
            android:textColor="#B0FFFFFF" android:layout_gravity="center"
            android:layout_marginTop="4dp" android:layout_marginBottom="24dp" />

        <EditText android:id="@+id/etRegUsername"
            android:layout_width="match_parent" android:layout_height="52dp"
            android:background="@drawable/bg_input_glass"
            android:hint="用户名" android:textColor="@color/white"
            android:textColorHint="#80FFFFFF"
            android:paddingHorizontal="20dp" android:textSize="15sp"
            android:inputType="text" android:maxLines="1"
            android:drawableStart="@drawable/ic_user" android:drawablePadding="12dp"
            android:drawableTint="#80FFFFFF" />

        <EditText android:id="@+id/etRegNickname"
            android:layout_width="match_parent" android:layout_height="52dp"
            android:background="@drawable/bg_input_glass"
            android:hint="昵称" android:textColor="@color/white"
            android:textColorHint="#80FFFFFF"
            android:paddingHorizontal="20dp" android:textSize="15sp"
            android:inputType="text" android:maxLines="1"
            android:layout_marginTop="14dp"
            android:drawableStart="@drawable/ic_nickname" android:drawablePadding="12dp"
            android:drawableTint="#80FFFFFF" />

        <EditText android:id="@+id/etRegPassword"
            android:layout_width="match_parent" android:layout_height="52dp"
            android:background="@drawable/bg_input_glass"
            android:hint="密码" android:textColor="@color/white"
            android:textColorHint="#80FFFFFF"
            android:paddingHorizontal="20dp" android:textSize="15sp"
            android:inputType="textPassword" android:maxLines="1"
            android:layout_marginTop="14dp"
            android:drawableStart="@drawable/ic_lock" android:drawablePadding="12dp"
            android:drawableTint="#80FFFFFF" />

        <EditText android:id="@+id/etRegConfirm"
            android:layout_width="match_parent" android:layout_height="52dp"
            android:background="@drawable/bg_input_glass"
            android:hint="确认密码" android:textColor="@color/white"
            android:textColorHint="#80FFFFFF"
            android:paddingHorizontal="20dp" android:textSize="15sp"
            android:inputType="textPassword" android:maxLines="1"
            android:layout_marginTop="14dp"
            android:drawableStart="@drawable/ic_shield" android:drawablePadding="12dp"
            android:drawableTint="#80FFFFFF" />

        <Button android:id="@+id/btnRegister"
            android:layout_width="match_parent" android:layout_height="52dp"
            android:background="@drawable/btn_gradient_round"
            android:text="注 册" android:textColor="@color/white"
            android:textSize="16sp" android:textStyle="bold"
            android:layout_marginTop="24dp" />

        <TextView android:id="@+id/tvSwitchToLogin"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:text="已有账号？去登录" android:textSize="14sp"
            android:textColor="#B0FFFFFF" android:layout_gravity="center"
            android:layout_marginTop="20dp" android:padding="8dp" />
    </LinearLayout>
</FrameLayout>
